---
type: task
mode: EXECUTION
lifecycle: living
scope: engineering
status: ready
---

# 陈展 Demo 逐页前端实现

## 当前轮：结束卡圆环完成后停留 3 秒（2026-09-14）

用户以结束卡截图明确：反馈完成进入“暂无新的任务”页面后，绿色圆环按现有 3.6 秒走满；走满后保持完整完成态 3 秒，再开始回到唤醒页的既有转场。该规则复用到所有会进入通用结束卡的故事，不改变圆环速度、样式、结束卡文案或回流转场结构。Missing: none，Conflicts: none。

本轮结果：通用结束卡配置新增 `completedHoldDurationMs = 3000`，回流计时改为圆环加载 3.6 秒与完成态停留 3 秒之和，既有约 3.2 秒回流转场不变。`npm run build` 与 `npm run preview:sync` 通过；5186 连续采样确认圆环约 3.6 秒走满、6.24 秒仍保持 `ending` 完成态、约 6.5–6.6 秒进入 `wake-return`，控制台无告警。本任务回到 `ready`。

## 当前轮：清理观众可见的 Mock 标识（2026-09-14）

用户重申数据与内容可以由 Mock 驱动，但 Demo 前台文案不得出现“Mock”或“Demo Mock”等实现标识。本轮仅清理观众可见文本，并在完整设计系统明确该边界；保留源代码命名、注释、任务记录和 Owner 中用于说明真实能力边界的 Mock 表述，不改变流程、数值、状态或设备能力。Missing: none，Conflicts: none。

本轮结果：空气异常决策项改为“空气消杀机在线，可执行本轮低扰动空气优化”，记忆依据改为面向观众的服务记录表达；完整设计系统明确 Mock 术语不得进入任何观众可见标题、正文、列表、语音、反馈、记忆详情或状态。构建与预览同步通过；5186 浏览器核对决策页和记忆详情均无可见 “Mock”，控制台无告警。本任务回到 `ready`。

## 当前轮：空气异常改为 CO₂ 偏高（2026-09-14）

用户要求将空气异常的核心异常点由 PM2.5／颗粒物改为更符合家庭日常的 CO₂ 偏高，同步调整感知证据、分析、决策、执行、反馈、记忆和语音；并明确暂不加入开窗／通风提醒。流程收敛为启动空气消杀机低扰动空气优化 Mock、等待设备运行回执、持续监测 CO₂，再由后续读数确认回落。依据事实 Owner 的能力边界，本轮体验不等于空气消杀机降低 CO₂ 的真实能力验收。复用现有页面、组件、黄色语义、语音球三态和转场；不改默认正常故事。Missing: none，Conflicts: none。

本轮结果：空气异常场景已切换为 CO₂ `986 → 1680 → 920 ppm` 连续 Mock，PM2.5 保持 `18 μg/m³`、颗粒物保持稳定；决策、执行、反馈、趋势、记忆和语音均改为低扰动空气优化、设备运行回执与 CO₂ 后续读数，不含开窗／通风提醒。代码仍明确标记真实降低 CO₂ 能力未验证，未改语音球组件或默认正常故事。`npm run build` 与 `npm run preview:sync` 通过；5186 浏览器核对决策、执行、反馈完整，页面无开窗／通风内容，控制台无告警。本任务回到 `ready`。

## 当前轮：语音球语义色层次与辉光收敛（2026-09-14）

用户要求保持正常蓝紫青语音球不变，将普通异常黄色与紧急红色由近乎单色改为以各自主色为核心的深浅渐变色系，可加入少量相邻配色丰富层次；同时收紧 `thinking` 与 `responding` 的辉光、强度和光带粗细，减少模糊，仍保留与 `idle` 可明显辨识的速度、频率与振幅差异。范围限于 `VoicePresence.tsx`、完整设计系统与受影响状态验证；不改语音球材质、位置、尺寸、正常色板、语音文案或页面流程。Missing: none，Conflicts: none。

本轮结果：`warning` 已改为棕橙暗部、琥珀／暖黄主色、暖黄高光与少量低饱和梅红过渡，`emergency` 已改为酒红暗部、鲜红／珊瑚主色和少量洋红过渡。三种语义色共用收敛后的 `thinking` / `responding` 光效参数，正常蓝紫青色板未改。浏览器已核对正常、普通异常和紧急三色的思考／播报态；光带边缘、中心曝光和色彩层次通过，控制台无告警。`npm run build` 与 `npm run preview:sync` 通过，5186 已加载最终构建，任务回到 `ready`。

## 当前轮：语音球动作状态统一（2026-09-14）

用户确认语音球统一为三态：没有交互时保持默认；思考／分析时进入明显的思考动态；语音播报时进入更强的播报动态，结束后恢复默认。范围为现有 `VoicePresence`／`VoiceDock` 参数与调用状态、完整设计系统同步及受影响页面验证；不改变语义配色、球体材质、位置、尺寸、语音文案或页面流程。Missing: none，Conflicts: none。

本轮结果：`VoicePresence` 已将频率、速度、振幅、光带数量、粗细、辉光和强度纳入三态参数；`VoiceDock` 支持无文字时持续呈现 `thinking`，正常与疑似跌倒分析页均已接入。浏览器验证待机 `idle`、思考分析 `thinking`、决策播报 `responding` 及播报结束恢复 `idle`，且语义配色独立、控制台无告警。`npm run build` 与 `npm run preview:sync` 通过，任务回到 `ready`。

## 当前轮：执行文案校正（2026-09-14）

用户通过浏览器批注明确两处执行文案：疑似跌倒持续守护改为“持续守护，记录实时状态”；人体明显不适执行目标改为“请先停止活动，保持休息。我会持续观察，并同步预设紧急联系人。”。随后补充疑似跌倒第二次询问需完整提示“我没事”和“我需要帮助”两种可说回答。本轮仅修改对应 Mock 文案并同步故事 Owner／完整设计系统，不改组件、布局或外部协同能力边界。验收为构建、5186 预览同步及页面文案核对；Missing: none，Conflicts: none。

本轮结果：三处 Mock 文案已更新，`npm run build` 与 `npm run preview:sync` 通过。5186 浏览器核对二次询问完整展示两种可说回答、跌倒执行页展示“持续守护，记录实时状态”、人体明显不适执行页展示新的执行目标；未修改组件、布局或外部协同状态。本任务回到 `ready`。

## 当前轮：疑似跌倒页面与流程纠偏（2026-09-11）

用户以三张截图和 Figma `329:69` 明确纠偏：所有跌倒页面保留全局时间／天气／Wi-Fi 与底部主菜单；顶部中心组件复用现有规范且不切红；确认页移除额外证据卡；仅确认弹窗压暗背景，后续分析／决策／执行恢复正常页面亮度；求助与无人回应执行阶段不得在左侧叠加协同卡，改为右侧执行目标与过程组件，流程收敛为保持当前状态、持续记录守护、同步预设紧急联系人、继续等待现场介入。Direct References 为疑似跌倒故事 Owner、完整设计系统、用户截图和 Figma `329:69`；Missing: none，Conflicts: none。验收页面结构、组件复用、文案／语音、两路执行状态、正常路径回归、构建与 5186 浏览器检查。

本轮结果：已恢复所有跌倒页面的品牌、Wi-Fi、日期天气和底部主导航；顶部 `AiosStatus` 保持通用绿色状态，不随紧急事件染红。确认页删除额外证据卡，复用主动确认组件的 `blur(20px)` 毛玻璃和红色语义；分析、决策、执行页恢复统一 `30%` 背景遮罩。执行页删除左侧协同卡，右侧按“执行目标 → 同步紧急联系人 → 持续守护”排列；联系人状态由准备、通知中到“通知已发出，等待现场介入”，完成时仅红色圆环走满并保留同步图标，不伪造联系人确认。头像浮窗恢复重新开始、上一步、下一步及三个异常故事入口，只在确认未回答、转场和持续守护等必要节点禁用跳步。`npm run build`、`npm run preview:sync` 通过；5186 浏览器验证完整页头／导航、通用顶部状态、确认毛玻璃样式、分析／决策亮度、三卡顺序、圆环完成态、菜单内容及无运行告警。

## 当前轮：疑似跌倒前端（2026-09-11）

用户已授权按疑似跌倒故事与设计系统还原。Direct References 为 `docs/architecture/exhibition-stories/suspected-fall-emergency.md`、总架构、事实 Owner 和完整设计系统第 23 节；沿用已实现的 Figma 312:359、314:467、319:607 组件。Missing: none；Conflicts: none。实现全局抢占、两次无人回应、三分支、协同发起与升级持续守护、确认没事后的记录和原流程恢复。验收构建、浏览器分支与恢复、正常路径回归；外部能力全部 Mock。

本轮结果：已新增全局疑似跌倒抢占层、可暂停恢复的原流程计时、射频疑似事件 Mock、三按钮与两次无人回应、三分支分析／决策、现场协同发起、红色持续守护、升级通知预设紧急联系人，以及“我没事”的蓝色确认反馈、短期记忆和原流程恢复。`npm run build` 与 `npm run preview:sync` 通过。5186 浏览器实测覆盖正常感知页抢占、人体主动确认抢占、记忆内页让位、三按钮焦点、两次无人回应、分析静默、求助执行、协同发起、守护升级、升级后不结束、“我没事”反馈／记忆／计时恢复和 Escape 只关闭菜单；控制台无错误。未单独重跑空气异常页抢占、普通语音展开中抢占、全部稳定页面组合、固定 1920／1280 视口和实体电视遥控器；真实射频、语音识别、现场协同、联系人通知与外部回执仍未接入。

> 读取提示：下方带日期的“当前轮／本轮结果”是历史实施证据，不拥有现行故事或设计定义；其中旧“体验架构第 N 节”章节号已随四故事 Owner 拆分失效。开始新一轮只读取本任务契约、总体体验架构、当前故事 Owner、完整设计系统和当轮 Figma。

## 当前轮：语音职责与确认间加载（2026-09-11）

用户现已确认五组决策语音，其中休息路径使用“您先在原地休息，我会持续关注，如有必要，15分钟后我会再次确认”。本轮接入配置与结论后播报；长文完整展示后才允许自动推进，保留原卡片文案和设备／提醒 Mock 边界。

接入结果：五组文案已配置，决策结论后展开；VoiceDock 按实测宽度在短文阅读或长文单向展示结束后通知推进，切页复用原收合。构建及 5186 同步通过；浏览器核对“没有／不确定”新原文、黄色 responding、14 秒滚动、自动进入陪护并恢复 voice-rest／idle，无错误日志。其他四组复用同一编排与组件，本轮未逐条重跑；文案及规则已吸收到设计规范第 13.1 节并由架构引用。

绑定本轮三项优化：所有思考／分析页取消展开播报并回到待命球；所有决策页设计口语化告知，文案待确认后接入；主动确认两问之间按 Figma `319:607` 增加加载状态，复用弹窗、语音球与蓝／黄／红语义。沿用下方 Context Manifest 和三份 Owner；用户本轮要求替代旧的分析播报／决策收合约定。验收本轮转场、重复提交与退出清理、构建和预览；不接入真实语音、模型或现场求助。

本轮结果：分析播报已取消；两问间原稿白色点环已做成 1.8 秒连续旋转，并随加载内容进出，保留语义色与单一球体。浏览器截图核对加载布局，连续采样确认转圈、加载时无答案按钮、第二问恢复按钮焦点；空气分析页核对 voice-rest、idle、warning、空文案，无错误日志。修复语音收合计时被 prelude 参数变化取消的问题。构建与 5186 同步通过，规范已通过维护任务原地吸收；蓝／红加载仅复用既有语义配置，未作为独立可运行分支验证。决策语音五组已按用户最终文案接入，休息分支完成浏览器查验；真实音频及外部求助未接入。

## 当前轮：紧急状态环及文案评审（2026-09-10）

用户已确认执行页提案，并提供理解／决策最终文案；本次按原文替换两处 Mock 文案，同步体验架构中的对应文案，不新增现场求助能力或协同回执。

确认后结果：两处文案已替换并同步体验架构；TypeScript／Vite 构建与 5186 预览同步通过。本次仅文案修改，未重复浏览器视觉验证。

绑定用户三张截图：修正明显不适决策摘要的状态环为紧急红色；执行目标及决策简化文案先提出供确认，不直接替换已确认主文案。沿用当前 Context Manifest、三份 Owner 与原状态环形状；验收语义颜色、构建和预览。

结果：状态环改用原尺寸 SVG 与明确语义色，移除滤镜近似变色；构建及 5186 预览同步通过。浏览器截图及计算样式确认紧急圆环为 `#ff3948`，无滤镜，正常绿色与普通异常黄色配置保留。两处主文案未替换，待用户确认精简提案。

## 当前轮：用户截图四项组件校正（2026-09-10）

按用户四张截图为感知状态复用彩色圆环；思考长文自动换行并撑高卡片，短决策按内容收缩；仅最终结束卡恢复常规绿色环与蓝色语音球，不改写异常反馈证据或已确认文案。沿用三份 Owner 与现有 Context Manifest，截图为直接来源。验收构建、预览、长短文本布局及最终结束态。

结果：四项已实现并同步 5186。浏览器核对感知两种状态均含旋转彩环、标题无横向溢出；身体思考正文两行、宽 368px 无横向溢出，卡高约 218px；短决策卡约 146px；最终结束卡为绿色环、normal 语音语义。构建通过，浏览器无运行错误。规则已原地更新设计规范与体验架构，正文未改写。

## 当前轮：身体不适前端还原（2026-09-10）

用户已授权身体不适前端还原。已确认方案现由[人体不适故事](../architecture/exhibition-stories/body-discomfort.md)和设计规范第 22 节拥有。范围为身体不适入口、Figma `312:359` 主动交互弹窗与 `314:467` 轻量陪护页；复用正常与空气异常组件、语音、转场，不做真实语音、设备、联系人或定时提醒接入。

验收：三路跳转与退出、两问不自动代答、单一活动语音球、状态语义、有限／无限陪护环、反馈与记忆来源一致、正常／空气异常回归、构建及浏览器检查。Direct References 增加上述两个 Figma 节点与人体不适故事 Owner。

本轮结果：身体不适入口与两问三路 Mock 已实现。浏览器检查了完整感知到首次确认、无不适直接反馈／结束、短时陪护到正常指标／趋势／记忆／回流，以及红色持续陪护超过短时周期后仍停留、下一步禁用、功能页可返回。最后校正了确认字体继承、底层待机残留、画布焦点滚动、单项反馈颜色及本轮记忆隔离。构建与 5186 常驻预览同步通过，正常回流后的自动路径继续运行；空气执行和反馈进行了查验入口回归。浏览器日志未见运行错误。视觉核对以当前浏览器缩放画布为准；独立 1920／1280 视口覆盖未记为通过。真实语音、设备、医疗阈值与外部通知未验证。

确认内容已通过维护任务吸收到三份 Owner；最后补齐功能内页打开时隐藏确认弹窗的可访问性状态，构建和常驻预览已再次同步。本任务回到 ready，等待本页评审或后续疑似跌倒设计。

## 当前轮：空气环境异常分支设计与实现

2026-09-09 用户先确认空气异常设计提案，随后确认直接继续前端实现。范围为：在不改动默认正常故事的前提下，为头像浮窗增加三个异常故事入口，启用空气环境异常；复用现有感知、思考／分析、决策、执行、反馈、记忆、结束与回流页；通过场景 Mock 传入黄色事件语义、异常指标、进度和语音状态；明确设备测量、待执行动作、设备回执和已确认结果。当时人体不舒服与疑似跌倒为待设计禁用入口；身体入口现已按上方当前轮启用。

空气异常的当前依据已收敛至事实 Owner 第 4–6 节、[空气环境异常故事](../architecture/exhibition-stories/air-environment-warning.md)和设计规范第 21 节；交付为 `demo/aios-exhibition/` 空气异常成功 Mock 分支。

本轮结果：已实现 `normal | air-warning` 场景配置、PM2.5 `18→86→32 μg/m³` 与颗粒物异常证据、黄色通用卡片／单项指标／处理进度／语音语义、空气消杀机指令和回执边界、成功反馈回到正常色、场景化短期记忆、结束回流后恢复默认正常故事。人体感知结果正常，睡眠带采集进度保持绿色。`npm run build` 通过，5186 常驻预览已同步；浏览器核对异常指标强调、思考与执行进度、异常语音框及最终恢复正常语音状态均通过。

细节校正：异常指标改为整行黄色；执行状态列扩展到 `96px`，“指令已发送”和“持续观察”完整显示；普通异常运行中状态点与文字统一为黄色。构建、常驻预览同步及浏览器视觉核对通过。

## Goal

依据用户逐轮提供的 Figma、当前故事 Owner 与完整设计系统，维护默认正常体验并逐步实现三个查验故事；不因异常分支改写默认正常路径。

## Sources

- `docs/knowledge/products/aios-exhibition-context.md`
- `docs/architecture/aios-exhibition-experience.md`
- `docs/architecture/exhibition-stories/README.md`；每轮只加载当前实现故事的 Owner。
- `docs/specs/aios-exhibition-design-system.md`
- 2026-09-04 用户本轮说明。
- Figma 文件 `ILpT5meWxj9quaJgdvXOqT`：基础组件 `267:2`；静默待机首页 `216:306`；感知唤醒页 `234:3`；落座感知页 `237:591`；分析页 `249:228`；思考前置阶段 `270:872`；决策页 `254:2`；执行页 `256:394`；反馈页 `260:535`；结束卡 `299:2`；设备内页 `237:335`；空间场景包 `297:533`；空间建模 `301:146`；其他实体 `301:230`。
- Figma 文件 `ILpT5meWxj9quaJgdvXOqT`：记忆功能基础框架 `262:87`；记忆内容、状态、详情精简及语音修正交互以 2026-09-09 用户本轮确认内容为直接来源。

## Scope

当前已实现基线包括正常故事、空气异常成功 Mock、人体不适两问三路、疑似跌倒首版 Mock、共用页面与设备／空间／记忆功能内页。每轮只实现用户明确授权的当前故事或页面；沿用已确认视觉来源和文案，使用合理连续 Mock；语音区域使用用户指定的 React Bits Strands。功能内页覆盖当前流程，不恢复旧 Demo 或直接发布。跨 Owner 吸收切换维护任务。

## Deliverables

- 当前授权故事在本地完整可运行，并复用待机、感知、分析、决策、执行、反馈、结束、功能内页和基础组件。
- 页面节奏与转场符合完整设计系统；紧急抢占使用专项节奏，不机械套用普通页面停留。
- Mock 数据与视图分离。
- Figma 对照和本地验证结果。

## Acceptance

### Hard Constraints

- 有 Figma 节点的页面先读取实际节点后实现；无 Figma 节点的页面以用户截图及本轮明确确认内容为直接来源，基础组件、布局、素材与文案对应来源。
- 待机状态无体验者，不伪造人物体征或真实设备联调结果。
- 电视入口仅反馈“待接入”；设备入口按本轮 Figma 实现，其余未提供功能内页不扩展。
- 文案或设计含义冲突先与用户确认。
- 构建与本页视觉、交互验证通过，按范围说明未验证事项。

### Soft Evaluation

- 组件可按设备、指标和状态配置；页面比例按实际 Figma 提取。

## Current Round

2026-09-04：已读取入口、三份核心 Owner、产品定位与系统原则，以及两个 Figma 节点。仓库目前只有文档，无前端工程。页面设计上下文已返回代码、素材链接与截图；基础组件总画板返回结构，后续按具体组件节点继续提取视觉参数。

## Context Manifest

Current Task: `docs/tasks/exhibition-frontend.md`

README Chain:
- `README.md`
- `docs/README.md`
- `docs/tasks/README.md`
- `docs/knowledge/README.md`
- `docs/knowledge/products/README.md`
- `docs/architecture/README.md`
- `docs/specs/README.md`

Direct References: 陈展事实 Owner、总体体验架构、完整设计系统、当前故事 Owner与本轮用户提供的 Figma；不加载无关故事 Owner。

Owner Closure:
- `AGENTS.md`
- `docs/RULES.md`
- `docs/canonical-vocabulary.md`
- 陈展事实 Owner、总体体验架构、当前故事 Owner与完整设计系统
- `docs/knowledge/products/origin-home-eldercare.md`（仅定位与系统级原则；其他业务场景和上位生态细节排除出本轮范围）

Missing: none for current ready state。疑似跌倒首版 Mock 已完成；专属 Figma 逐页确认、真实射频与外部协同不属于本轮实现验收。

Conflicts: none。用户已确认五项导航逐步设计；电视仅待接入；页面不加演示标识。

本轮继续：使用 React + TypeScript + Vite，基础组件优先实现本页实际使用部分，其余随后续页面扩展。

## Round Result

2026-09-04：本轮待机首页与本页基础组件完成，构建及页面素材、文案、导航、提示核验通过。实际实现和验证边界已吸收到设计规范第 8 节。任务回到 ready，等待后续页面或本页评审反馈；不代表后续页面已实现。

## 本轮动效调整

用户提供 circular-dial-loop-animation.zip 与两张截图，授权待命省略号循环、彩环旋转、透明底雷达替换、四项文案自上而下亮起放大。仅修改首页动效；视觉大小保持，未新增流程。

动效轮结果：已接入用户 Canvas 动画，完成省略号、彩环与文案轮播；构建与浏览器检查通过，规范已吸收到设计系统动效补充。

## 语音组件接入

2026-09-04 用户指定 React Bits Strands-JS-CSS 及链接参数。替换右下角语音图形，支持默认、思考分析、执行/回应状态以及正常、普通异常、紧急配色；当前首页仅默认正常，不新增页面或麦克风接入。

语音轮结果：官方 Strands JS/CSS 接入，默认正常状态显示；其余动作与场景配置为后续可调初值，规范已记录，构建和默认页面核验通过。

## 玻璃球材质调整

用户授权语音球外壳改为暗系渐变与光影质感，球内动效保持不变；仅修改 Strands 玻璃合成层。

材质轮结果：已完成暗色玻璃球合成层调整，构建通过，球内动效参数未改变。

## 感知唤醒页

2026-09-04：继续实现 Figma `234:3`。范围为待机页完整停留 10 秒后模拟检测到体验者，再自然过渡：旧内容模糊淡出、顶部状态激活、左上家庭康养包加载、右侧发现卡进入、语音球移位后展开问候输入。首轮 Mock 遵循单体验者规则，显示 `01 人`；不新增后续页面或真实射频接入。

唤醒轮结果：完成 10 秒待机与约 2.8 秒分阶段过渡；Figma 人物及发现图标已本地化，构建和浏览器最终布局、文案、素材及运行错误核验通过。实现与时序已吸收到体验架构第 8 节、设计规范第 9 节。任务回到 ready，等待下一页或评审反馈。

## 唤醒页视觉校正

2026-09-04：按用户截图校正加载进度、状态条透明度与省略号节奏、发现卡素材，以及语音球缩放移位后向左展开输入框的顺序。素材以用户提供的 `归档.zip` 为准。

校正结果：进度条拉满、扫光改为 2 秒周期、省略号改为 1.8 秒周期，唤醒彩环使用 100% 不透明素材；发现卡两项素材与压缩包校验一致；语音球落在输入右端并由右向左展开输入。构建与浏览器最终尺寸、素材、状态核验通过，任务回到 ready。

2026-09-04 二次校正：重新以 Figma `234:3` 的语音球 x=1432、y=954、64×64 和输入框 x=1432、y=952、428×68 为准；撤销上一轮对展开方向的错误理解。整体唤醒过渡调整为 3.4 秒，打字作为最后阶段单独出现。

二次校正结果：语音球、输入框、文字起点均按 Figma 坐标恢复；球内画布收至 78×78 以匹配 64×64 可见球体。过渡 3.4 秒，最后 720ms 单独用于打字并增加临时光标。构建通过，浏览器按页面缩放比例核对最终边界与左对齐关系，无运行错误；任务回到 ready。

2026-09-04 语音球轨迹复核：重新比较静默页 `216:306` 与唤醒页 `234:3` 的语音球可见尺寸、裁切框、中心点和位移，按两页真实视觉变化修正平移缩小过程。

复核结论：静默框 94×98、中心 `(1817,971)`；唤醒框 64×64、中心 `(1464,986)`。改为稳定 WebGL 画布、整体缩放 0.67，并在父级使用实际裁切框；移动结束前保持 idle，完成后切换 responding，避免移动阶段重建视觉状态。

2026-09-04 底部对齐校正：底部导航不再采用唤醒稿 x=742，所有页面固定为页面中心 x=714；静默语音球下移至 y=937，使其中心与导航、唤醒输入框及唤醒语音球统一为 y=986。唤醒时沿同一水平中心线平移并缩小。

对齐结果：构建通过；浏览器核对唤醒页导航、输入框、语音球纵向中心完全一致，导航中心与页面中心一致。静默页按固定框位计算为同一 y=986 中心线，任务回到 ready。

复核结果：构建通过；浏览器确认唤醒最终父级裁切框为 Figma 的 64×64、x=1432、y=954，输入框与球体左边缘一致，WebGL 画布保持 160×160 并应用 0.67 整体缩放，父级 overflow 裁切生效，无运行错误。任务回到 ready。

## 落座感知页

本轮范围扩展至 Figma `237:591`：模拟体验者落座后开始人体与空间感知；还原本页组件，顶部状态条从旧形态自然扩展，统一语音退场与复位。延续 10 秒静默、当前页约 3.4 秒参考转场及导航居中规则，不扩展其后页面。上下文沿用现有 Manifest 与三份核心 Owner；新增直接来源为本轮用户说明及该 Figma 节点。

设备范围校正：当前没有血压仪，落座感知页移除血压仪及其发现状态，只保留睡眠带。

感知页结果：完成唤醒页稳定停留 10 秒后进入落座感知页，3.4 秒内完成旧内容退场、顶部状态外壳扩展、左右内容进入及语音退场复位再展开。空间信息保持单体验者连续 Mock，人体感知仅显示睡眠带 30%；构建通过，浏览器确认最终文案、组件、设备范围及运行状态，无控制台错误。任务回到 ready，等待下一页或本页评审反馈。

转场与适配补充：进入落座感知状态时先通过页面透明度变化整体亮起，再开始组件转场；设备卡高度必须随设备行数自适应，当前单条睡眠带状态下自动收缩。

补充结果：转场前 400ms 独立完成页面提亮，随后衔接旧内容退场、顶部形变和语音生命周期，总时长保持 3.4 秒；人体感知卡改为按设备行数自动伸缩。中心三个点保持逐点循环，绿色状态点增加呼吸闪烁。构建通过，浏览器分阶段采样确认时序、单行卡片高度、动效绑定与无血压仪残留，无运行错误。任务回到 ready。

长期协作原则已吸收到设计规范第 4、5 节：页面转场不设全局固定时长，以自然、连续、舒适为判断；逐页由 AI 主动提炼并优化可复用组件、交互和动效。现有约 3.4 秒只保留为当前页面实现记录。该轮仅修正规范和后续工作方式，不改变已验证页面实现，任务回到 ready。

任务契约同步修正为已实现三页及两段 10 秒稳定展示，后续页面边界从“唤醒页之后”更新为“落座感知页之后”；无新增实现范围。

状态省略号补充：落座感知页右侧“持续感知中”和“匹配设备能力”后的三个点需逐个循环出现，并复用现有省略号动效能力。

省略号结果：提取并复用 `CyclingDots`，顶部、待机状态及右侧两处状态统一使用 1.8 秒三段循环。构建通过；浏览器确认两处均为 3 个点且依次绑定 `standby-dot`、`standby-dot-two`、`standby-dot-three`，无运行错误。任务回到 ready。

## 当前轮：动态感知过程

2026-09-07 用户四张截图及过程说明作为直接来源。范围：空间摄像头与空气设备采集全部达到 100% 后，同一卡片变为蓝色结果；随后出现睡眠带采集卡，完成后变为人体结果。提取数据驱动设备进度、左右结果轮播、溢出列表上移与底部虚化。沿用本任务 Manifest 与三份核心 Owner；无缺失来源。验收顺序、100% 保留、两类结果、六项空气指标、单体验者、扩展性及固定预览同步；不进入第四页。

动态感知轮结果：已实现并同步常驻预览。构建通过；浏览器确认空间独立进度、100% 停留、空间结果后出现人体采集、最终双结果卡、两项体征与六项环境指标。规则吸收到体验架构第 9 节及设计规范第 10 节；无控制台错误。任务回到 ready，等待后续评审。

## 本地预览稳定性

2026-09-07：用户反馈 `127.0.0.1:5186` 拒绝连接。范围为定位进程、端口、HTTP 与启动方式，恢复预览并建立不依赖当前终端会话的稳定访问机制；不修改页面设计。

排障结果：5186 无监听进程，原 Vite 预览随先前会话结束，页面代码构建正常。已安装用户级 `launchd` 服务 `com.aios.exhibition.preview5186`，由持久缓存目录提供构建产物；新增安装与 `preview:sync` 刷新脚本。首页、JS、CSS 返回 200；终止服务进程后被自动拉起并恢复 200；浏览器确认页面进入 standby 且无控制台错误。技术规则已吸收到设计规范第 11 节，任务回到 ready。

## 当前轮：结果卡高度与间距

2026-09-07 用户截图校正：单结果无分页时按实际指标内容收缩卡片，维持底部内边距；来源标题至指标列表使用 30px 间距。沿用当前 Manifest，仅修改感知卡布局、同步规范和本地预览；验收浏览器尺寸与构建。

节奏补充：两类感知卡在全部设备达到 100% 后停留 2 秒，再开始转为结果卡。

本轮结果：无轮播结果卡高度按指标内容收缩，标题至列表间距改为 30px，全部进度满格停留 2s；构建和本地预览同步通过。浏览器已核对结果卡尺寸与间距，规则更新于设计规范第 10 节。

间距微调：用户将来源标题到指标列表间距由 30px 改为 24px；同步内容高度及设计规范，其余内边距不变。

微调结果：间距 24px、单结果卡高度 250px，浏览器尺寸核对及构建通过，常驻预览已同步。

## 当前轮：溢出指标预览

用户截图要求下一条未完整展示的指标隐约可见。调整指标窗口和淡化强度，为第三行及分页留空间；保持 24px 标题间距与单结果自适应规则。验收构建、浏览器第三行可见及分页不重叠。

溢出预览结果：扩展窗口容纳两条清晰指标与下一条淡化预览，底部轻微渐隐；分页预留空间，浏览器截图核对无重叠。构建通过，常驻预览与设计规范已同步。

## 当前轮：思考分析页

用户提供 Figma `249:228`；分析当前感知结果，动态展示调用能力、技能、设备与进度。感知全部完成并呈现结果后稳定停留 10 秒进入分析，复用自然转场和语音生命周期。用户授权重写说明性语音文案，长文缓慢向左展示；顶部支持多状态共存，感知持续亮起。沿用 Manifest，新增该节点及本轮用户消息为直接来源；验收设计还原、进度与阶段同步、10 秒停留、长文可读、多状态、构建与浏览器。前一页规范已吸收，未提供的后续页面排除。

本轮结果：完成感知结果页停留 10 秒后进入思考分析页；右侧三项能力随 12 秒分析进度逐项亮起并最终达到 100%，顶部感知与思考／分析同时亮起。语音使用 thinking 动效，说明文案在固定输入框内缓慢左移。构建、常驻预览与浏览器最终状态检查通过，无控制台错误；流程与组件规则吸收到体验架构和设计规范。

### 思考分析页细节校正

2026-09-07 用户截图与说明：thinking 语音提高速度和振幅；长文只向左展示一次，结束后输入框收合并恢复初始语音球；文字前后各留 24px。分析列表默认可见三条，超出三条自动向上展示；当前处理项逐条扫光，未完成为灰点、完成为绿点。进度区需保证 100% 不换行。沿用本轮 Figma 与 Manifest，不新增后续页。

校正结果：thinking 参数提高，长文单向展示后语音球复位；文案前后 24px。分析卡支持三行窗口、超出自动上移、逐项扫光和灰／绿完成点；100% 保持单行。构建与常驻预览同步通过，浏览器已核对最终状态和复位。

语音文字安全区修正：24px 指语音球之后至文字、文字至输入框右边缘的滚动安全距离；未溢出文案在该安全区水平居中，只有实际宽度溢出的文案才从左安全线开始滚动。

安全区结果：组件改为按实际渲染宽度判断溢出；短文中心与安全区中心一致，长文使用 316px 可视区从左安全线单向滚动，末端保留 24px。构建和常驻预览同步通过，浏览器已核对短文居中与语音最终复位。

## 当前轮：头像查验控制

2026-09-07 用户要求点击右上头像显示小型下拉浮窗，提供重新开始、上一步、下一步，方便快速切换页面查验。稳定查验节点为待机、唤醒、感知、思考分析；手动切换停留目标页，重新开始恢复自动流程。不增加演示标识，不扩展业务页面。验收开合、边界禁用、流程计时清理、重启及浏览器切换。

本轮结果：右上头像已增加暗色玻璃下拉浮窗，支持重新开始、上一步、下一步。手动切换会清理旧页面计时，从待机、唤醒、感知、思考分析四个稳定查验节点重新运行后续流程；重新开始回到静默待机并恢复完整自动流程。首尾边界按钮自动禁用，点击页面外或按 Escape 可关闭。

### 查验切换流程修正

2026-09-07 用户明确：手动切换仅改变当前查验起点；该页面自身流程完成后，应继续进入下一页，不持续停留。待机页继续执行 10 秒唤醒节奏，唤醒页继续执行 10 秒后落座感知，感知页完成采集并停留 10 秒后继续思考分析；当前思考分析之后尚无已实现页面，因此停留在其最终状态。

修正结果：已移除手动切换后的持续停留逻辑，为各查验节点恢复对应的后续计时和完成回调。构建与 5186 常驻预览同步通过；浏览器实测手动切到唤醒页后依次进入落座、感知，并在完整感知流程结束后进入思考分析页。

## 当前轮：语音文案虚实过渡

2026-09-07 用户截图反馈语音输入框文案出现和移动较生硬。范围为文案进入时模糊淡入、长文滚动窗口两端渐隐、结束时模糊淡出后再收合输入框；保持现有文案、安全距离、滚动方向与速度。

本轮结果：文案进入增加 520ms 模糊聚焦，长文可视窗口左右各使用 14px 渐隐过渡，退出改为 360ms 模糊淡出后再收合输入框。浏览器核对长文滚动、渐隐遮罩和页面切换正常；同时修正页头层级，使待机、唤醒等页面的头像查验入口都能稳定点击。构建及 5186 常驻预览同步通过。

## 当前轮：人体感知语音触发

2026-09-07 用户明确感知页语音文案主要针对人体感知：空间感知阶段语音球保持初始状态，空间结果完成并启动人体感知时再移动、缩小和展开文案。同时将长文窗口两端的完全渐隐调整为部分透明，减轻边缘黑色压暗感。

本轮结果：感知页语音状态已由人体感知启动回调驱动；空间感知期间保持 `voice-rest`，人体卡出现时进入 `voice-enter`，随后自然展开至 `voice-shown`。长文两端遮罩由完全透明调整为 45% 可见。构建和 5186 常驻预览同步通过；浏览器按阶段核对语音状态、人体卡出现时机及长文遮罩均符合要求。

## 当前轮：思考前置过程

2026-09-07 用户新增 Figma `270:872`：仍使用当前思考／分析页面，先动态展示思考过程，进度达到 100% 后再自然转为现有分析过程；思考条目沿用逐条扫光加载表达。范围仅扩展本页内部阶段，不进入决策等后续页面。验收 Figma 对照、阶段顺序、100% 转换、扫光状态、语音与顶部流程连续性、构建及浏览器。

本轮结果：新增 8 秒思考进度及 100% 后 1 秒确认停留，随后用 700ms 完成同一右侧卡片由 428×188 向分析卡高度扩展、思考内容模糊淡出和分析内容清晰进入。思考文案与状态点持续扫光／逐点循环；思考阶段语音球保持初始状态，分析阶段出现后才执行分析语音的移动、缩小、展开及播报。Figma 原始思考图标已本地化。构建及 5186 常驻预览同步通过；浏览器已核对思考进度、阶段转换、分析进度重置及语音触发顺序。

## 当前轮：决策流程页

2026-09-07 用户提供 Figma `254:2`：思考分析完成后稳定停留 8 秒，再经自然转场进入决策流程；此前各页面稳定停留统一由 10 秒调整为 8 秒。决策页包含毛玻璃分析结果组件与实色渐变最终理解／决策组件；结果逐条出现，三条窗口，超出自动上移且底部条目虚化。决策结论保持简短，必要时高度自适应。状态色语义为正常蓝、异常黄、紧急红。本轮实现正常路径 Mock，不扩展异常与紧急业务分支；同时沉淀已确认的思考分析和决策范式。

本轮结果：完成思考／分析页到决策页的自动衔接，各页面稳定停留统一为 8 秒。决策页逐条呈现 6 项连续 Mock 分析结果，三行窗口自动上移并在底部渐隐，随后显示正常蓝色的理解／决策卡；黄色、红色变体保留为数据驱动状态，未扩展对应业务分支。头像查验控制新增决策节点。TypeScript 与 Vite 构建、5186 常驻预览同步和浏览器自动时序均通过；实测分析进度 100% 后约 8 秒进入 3.2 秒决策转场，最终无缺图或控制台告警。流程与组件规则已吸收到体验架构第 11 节和设计规范第 13 节，任务回到 ready。

## 当前轮：决策页视觉校正

用户截图指出“状态正常”应沿用 Figma 的绿色彩环，不使用自定义蓝色圆点；上下两张决策卡的标题图标当前不可见，并需检查同类页面卡片图标。范围为按 `254:2` 原稿恢复状态与图标视觉，排查通用样式影响并浏览器复核，不改变流程、数据或文案。

补充：决策结果超过三条时，首次逐条加载完成后持续循环上移，避免稳定停留阶段只能看到最后三项环境分析。

校正结果：重新读取 Figma `254:2` 并使用节点截图替换此前全透明的错误素材；思考、分析和决策标题图标均恢复为原稿 28×28 图标，并通过新文件名避开浏览器旧缓存。决策状态恢复为 Figma 18×18 绿色圆环，“状态正常”使用 100% 白色。六项结果在首次加载后按 2.4 秒逐行持续循环，实测可从环境结果无反向跳动地回到心率、呼吸。构建、5186 常驻预览和三个页面浏览器复核通过，无缺图或控制台告警，任务回到 ready。

## 当前轮：执行流程页

用户提供 Figma `256:394`：理解决策完成后稳定停留 8 秒，经自然转场进入执行。页面先显示实色渐变执行目标卡，再显示毛玻璃执行过程卡；过程按顺序动态展示 4 条，超过 4 条自动上移并在底部渐隐，最后同步进度条。延续正常蓝、异常黄、紧急红的事件状态语义，本轮只实现前后连续的正常路径 Mock；不扩展反馈页或异常业务分支。需同步沉淀已确认的执行流程、组件和动效规范。

本轮结果：已按 Figma `256:394` 接入执行页。决策结论完整形成后稳定停留 8 秒，再经约 3.2 秒自然转场进入执行；执行目标卡先出现，过程卡随后进入，4 项连续 Mock 动作依次从待处理转为运行和完成，服务进度最终达到 100%。顶部同时高亮感知与执行，语音输入框收合并保持初始语音球。正常蓝色路径已实现，黄／红变体由数据配置预留，未扩展业务分支。TypeScript、Vite 构建、5186 常驻预览和浏览器时序／视觉核验通过；规则已吸收到体验架构第 12 节和设计规范第 14 节。

细节校正：执行目标卡清晰出现后保持 1.6 秒，过程卡再进入；底部进度轨道收至 232px，为“服务进程 100%”保留安全间距，避免满格时文字与轨道挤压。

细节校正结果：常驻预览重新构建并同步；浏览器分段采样确认目标卡先出现、过程卡在 1.6 秒间隔后进入。完成态显示 4 条结果与 100%，进度轨道和右侧文案之间保留清晰空隙且无换行。

## 当前轮：反馈流程页

2026-09-08 用户提供 Figma `260:535`：执行流程完成后稳定停留 6 秒，再经自然转场进入反馈流程。页面包含实色渐变执行结果卡、毛玻璃结果反馈卡、4 条顺序反馈及可按事件配置的数据对比图；超过 4 条时自动上移并在底部渐隐。反馈状态按结果判断：恢复正常为蓝色，持续异常且需介入为黄色，持续紧急且需升级为红色。当前只实现正常路径的连续 Mock，并在全部反馈完成后展示 INT AIOS 收集有效日志、沉淀经验的语音表达；不扩展新的异常业务路径或反馈后的页面。

本轮结果：已按 Figma `260:535` 接入反馈页。执行进度达到 100% 后稳定停留 6 秒，再经约 3.2 秒自然转场；执行结果卡先出现，反馈卡随后进入，温度、湿度、CO₂、PM2.5 四项连续 Mock 逐条确认。新增 CO₂ 986→778 ppm 的 30 秒趋势图与可配置图表数据，全部反馈完成后才展开成长语音。顶部同时高亮感知与反馈，左侧显示成长日志入口。正常蓝色路径已实现，黄／红结果变体已预留但未扩展业务分支。TypeScript、Vite 构建、5186 常驻预览及浏览器视觉／时序核验通过；规则已吸收到体验架构第 13 节和设计规范第 15 节。

### 反馈卡间距校正

用户反馈新增“CO₂ 服务趋势”后内部较拥挤。保留趋势标题和可读摘要，适度增加反馈卡高度，并拉开四项结果、趋势标题、图表和卡片底部之间的间距；不改变数据、流程或状态语义。

图表细节补充：辅助线按 Figma 改为三条水平实线和 20s 位置单条纵向短虚线，移除多余纵向网格，并校正折线起止位置。

语音文案确认：沿用设计稿“我会收集日志，筛选数据作为成长经验”。

校正结果：反馈卡高度调整为 435px，四项数据到趋势标题保留 28px 间距，图表到底部保留 37px 空间；趋势图使用三条水平实线与 20s 位置单条纵向短虚线。语音文案已按确认内容更新。TypeScript、Vite 构建及 5186 常驻预览同步通过；浏览器最终核对图表、卡片间距和语音文案均符合要求，任务回到 ready。

## 当前轮：反馈结束与重新唤醒

2026-09-08 用户提供 Figma `299:2`：反馈页完成后稳定停留 8 秒，反馈结果卡与过程卡自然退场，语音输入框收合并恢复初始语音球；随后出现结束卡，右侧绿色圆环从无到有加载，3–4 秒完成后表示当前未感知到新的事件与需求，再经自然转场回到已有唤醒页并继续正常流程。范围不增加新的业务页面或事件分支；验收 Figma 还原、退场顺序、语音复位、加载节奏、回流状态、头像查验控制、构建及浏览器时序。

本轮结果：已完成反馈结束与循环回流。反馈内容及成长语音完成后稳定展示 8 秒，结果卡与过程卡退场、语音恢复初始态，随后显示结束卡；加载器使用 Figma 原始 `64×64px`、`4px` 底圈和 `26×26px` 沙漏，绿色 `4px` 进度描边在 3.6 秒内完成。加载结束后背景由 30% 平滑变暗至 50%，顶部任务内容先虚化消失，外壳再收窄降高并恢复唤醒文案，页面在约 3.2 秒内回到唤醒页并继续落座感知流程。TypeScript、Vite 构建及 5186 常驻预览同步通过；浏览器已核对 8 秒停留、退场顺序、加载进度、原稿图标、背景变暗、顶部形变及自动回流。规则已吸收到体验架构第 14 节和设计规范第 16 节。

## 当前轮：场景文案统一

2026-09-08 用户提供四张页面截图并确认：待机轮播“家庭康养”改为“康养场景”；唤醒加载文案改为“开始加载康养场景包”；后续流程页统一显示“康养场景已加载｜暂无空间建模设备”；感知页标题改为“INT AIOS 持续守候并建立人体与空间感知”。范围仅为已实现正常路径的文案统一与版式核对，不改变流程或数据。

本轮结果：四处文案已统一，并将流程页加载状态提取为共享文案，感知、思考分析、决策、执行、反馈与结束卡不再各自维护旧称。TypeScript 与 Vite 构建、5186 常驻预览同步通过；浏览器核对待机、唤醒、感知三个节点及感知页两行标题，长加载状态未超出左侧容器。相关事实已吸收到体验架构与设计规范，任务回到 ready。

## 当前轮：设备功能内页

2026-09-08 用户提供 Figma `237:335`：设备入口以毛玻璃弹窗覆盖当前页面；打开时当前页面内容模糊退场，只保留顶部中心状态组件和恢复初始态的语音球。非静默／唤醒页背景略微加深，静默与唤醒页维持当前暗度。设备首轮按设计稿 Mock，在线与离线状态决定开关是否可控。范围不扩展空间、记忆或电视内页；验收弹窗还原、任意流程页打开与关闭、语音复位、背景分级、设备状态和开关交互、流程恢复、构建及浏览器。

本轮结果：已按 Figma `237:335` 完成设备功能内页。设备弹层使用数据驱动的八项 Mock 和全部／在线／离线筛选；四项在线设备可切换开关，四项离线设备保持禁用。打开时当前主体模糊淡出，过程页背景加深至 50%，顶部状态继续显示，非初始语音自动收合为初始球；关闭后恢复当前流程。TypeScript、Vite 构建及 5186 常驻预览同步通过；浏览器已验证弹层布局、8／4／4 筛选、在线开关往返、离线禁用、过程页语音复位和退场恢复。规范已吸收到体验架构第 15 节和设计规范第 17 节，任务回到 ready。

## 当前轮：空间功能内页

2026-09-08 用户提供 Figma `297:533`、`301:146`、`301:230`：空间入口沿用功能弹层框架，包含平级的空间场景包、空间建模、其他实体三个子功能。场景包默认展示康养场景包，卸载操作弹出“当前系统默认，暂时禁止卸载”；空间建模显示暂无相关设备，并复用雷达动效；其他实体展示视觉或空间感知发现的沙发、桌椅等实体。范围不扩展真实场景包安装、真实建模或设备接入；验收三个子功能切换、Figma 视觉、禁用提示、空状态动效、实体展示、任意流程页打开关闭、语音复位、构建与浏览器。

本轮结果：已按三个 Figma 节点完成空间功能内页并启用底部“空间”入口。空间场景包展示默认康养包，点击禁用视觉的卸载入口会显示指定提示；空间建模复用现有雷达动效和无设备空状态；其他实体以配置化卡片展示“沙发椅｜客厅”。页面沿用功能弹层退场、背景和语音复位规则，场景包及实体数据已抽到 Mock 配置。TypeScript、Vite 构建和 5186 常驻预览同步通过；浏览器已验证三个子页、卸载提示、雷达 Canvas、实体素材、主页／Escape 关闭、空间与设备切换及流程中语音收合，无控制台告警。规则已吸收到体验架构第 16 节和设计规范第 18 节，任务回到 ready。

## 当前轮：主导航反色校正

2026-09-08 用户截图指出空间、设备等功能按钮切换后，白色选中胶囊中的图标未同步反色，出现图标不可见或状态不一致；随后补充空间功能侧栏选中态与空间建模图标必须按 Figma 原稿校正。范围仅校正主导航状态、空间弹层分区图层、侧栏选中态和建模图标，不改变内页流程；验收主页、空间、设备切换状态及浏览器视觉。

本轮结果：已移除导航 SVG 内重复携带的 `40%` 透明度，统一由按钮状态控制颜色：选中项为白色胶囊、图标与文字 `#191919`，未选中项为白色 `40%`。空间弹层按 Figma 恢复为透明左栏与白色 `10%` 右侧玻璃层，左栏当前项保持白色 `10%` 玻璃底；空间建模图标重新导出节点 `301:228` 的原始 `18×18px` 资源。TypeScript、Vite 构建和 5186 预览同步通过；浏览器已验证主页、空间、设备三种主导航状态，空间三个子项状态与建模图标，控制台无告警。相关规则已吸收到设计规范，任务回到 ready。

## 当前轮：记忆功能内页

2026-09-09 用户确认按左侧入口、中间记忆列表、右侧精简详情实现记忆页，中右栏比例可按内容调整。短期状态为新发现／验证中，长期状态为学习中／已形成／已沉淀；初期不展开完整事件和原始日志，询问、纠正等操作统一通过 AI 语音框完成。基础框架读取 Figma `262:87`，语音框按用户截图调整为纯黑大圆角并弱化默认提示文案。

本轮结果：已启用“记忆”入口，完成 `220 / 610 / 455px` 三栏布局、全部／短期／长期筛选、五条连续 Mock、精简详情和语音拟修改流程。首次形成和最近更新同时保留，事件时间线按需询问、不占右栏。TypeScript、Vite 构建和 5186 常驻预览同步通过；浏览器验证长期筛选为三条、详情联动、语音拟修改及确认反馈，无控制台错误。规则已吸收到体验架构和设计规范，任务回到 ready。

### 记忆页图标、字号与详情重叠校正

2026-09-09 用户指出左栏记忆图标被错误去色，明确提供 `Group 3.svg`；同时确认返回标题、分类标题、详情标题均为 `20px`，左栏功能“记忆”为 `18px`，数字为 `16px`。右栏内容出现叠压，要求认真核对正常文档流与留白层级。

校正结果：已使用用户提供的蓝底 `30×30px` SVG，仅替换左栏记忆功能图标；字号逐项按确认值设置。右栏重叠根因是详情内部 `<header>` 被页面全局 `header` 绝对定位规则命中，已改为普通内容容器，标题、三个字段组、时间和语音框恢复正常纵向排布。构建和 5186 预览同步通过；浏览器逐项测得 20／20／20、18、16px，确认各区块边界不相交、图标为 30px、控制台无错误。任务回到 ready。

### 记忆页视觉校正

2026-09-09 用户截图指出：记忆弹层比空间／设备更深，左栏及列表选中态偏离既有规范，三栏顶部字号不一致，右侧缺少明确的“记忆结论”字段且分割线过多、层级拥挤。校正后弹层明度与空间页一致，左栏改为中性当前态，列表移除蓝色渐变、侧条、位移和阴影；三个顶部标题统一为 `20px / 28px`。右栏补充“记忆结论”，保留此前确认的全部字段，移除内部横线并用 `21–24px` 留白区分层级。构建与 5186 预览同步通过；浏览器确认两内容栏均为白色 `10%`、详情无分割线、内容不侵占语音框、无控制台错误，任务回到 ready。

### 空间／设备／记忆共用组件校准

2026-09-09 用户要求以设备 Figma `237:335`、空间 Figma `297:533`／`301:146`／`301:230` 和既有沉淀规范为优先依据，统一核对空间、设备、记忆的组件与交互，并像主页流程一样补充可复用规范。范围只限三个功能内页，不改变主页或底层运行流程。核对发现空间侧栏和场景包操作文字因无效 `font` 简写回退为 `16px`，设备左右玻璃分层未完全复用空间原稿。校准目标为：共用 `1285×766px` 外壳、透明 `220px` 左栏、白色 `10%` 右内容层；标题 `20px / 28px`、侧栏 `18px / 25px`；保留各页内容结构和既有操作语义。验收包括 Figma 对照、三页实测字号／表面、筛选与返回、构建及控制台。

校准结果：设备右侧内容层改为白色 `10%` 玻璃、左栏恢复透明，与空间和记忆共享同一表面变量；三个页面的返回、内容标题和筛选统一为 `20px / 28px`，侧栏统一为 `18px / 25px`。修复空间侧栏及场景包操作区的无效字体简写，浏览器实测分别恢复为 `18px` 和 `14px`。长期记忆筛选、详情联动、语音拟修改及 Escape 关闭均通过；TypeScript、Vite 构建和 5186 常驻预览同步通过。共用组件／视觉规则已吸收到设计规范第 20 节，交互契约已吸收到体验架构第 17 节，任务回到 ready。
